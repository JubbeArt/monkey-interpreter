return {
	keys <- fn (rec)
		res <- [] 
	
		loop (key in rec)
			res.add(key)
		end
		
		res		
	end
		
	values <- fn (rec)
		res <- [] 
	
		loop (key in rec)
			res.add(rec[key])
		end
		
		res
	end
}
